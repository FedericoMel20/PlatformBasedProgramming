// add function to sum number
const addNumber = (number1,number2) =>{
    return number1 + number2
}
//Subtract
const subNumber = (num1, num2) =>{
    return num1 - num2
}
//Multiply
const mulNumber = (num1, num2) =>{
    return num1 * num2
}

module.exports = {addNumber, subNumber, mulNumber}